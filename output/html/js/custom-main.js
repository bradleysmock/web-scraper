// Prevents _satellite is not defined errors
_satellite = {
    _runScript1: () => {},
    _runScript2: () => {},
    __registerScript: () => {},
};

_global = {};

function details(courseId) {
    console.log(courseId);
}
